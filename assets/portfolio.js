//initializing buttons
$(document).ready(function () {
    $('.fixed-action-btn').floatingActionButton();
});

// for parallax image, initializing
$(document).ready(function () {
    $('.parallax').parallax();
});

// intitializing floating action button
$(document).ready(function () {
    $('.fixed-action-btn').floatingActionButton();
});